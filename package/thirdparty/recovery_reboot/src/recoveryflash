#!/bin/sh
IMAGE="$1"
if [ -z "${IMAGE}" ]; then
exit 1
fi
if [ ! -f "${IMAGE}" ]; then
exit 1
fi
cp -f "${IMAGE}" /recovery/update.zip || exit 1
echo "--update_package=/cache/update.zip" > /recovery/tlbb_ota.upg || exit 1
reboot recovery
